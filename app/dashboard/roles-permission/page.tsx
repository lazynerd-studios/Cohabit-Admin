import Registry from "../registry";
import Roles from "@/components/roles-permissions/Roles";

const page = () => {
  return (
    <Registry>
      <Roles />
    </Registry>
  );
};

export default page;
