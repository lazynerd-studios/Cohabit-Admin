import Registry from "../registry";
import Charges from "@/components/charges/Charges";

const page = () => {
  return (
    <Registry>
      <Charges />
    </Registry>
  );
};

export default page;
