import Registry from "../registry";
import Settings from "@/components/settings/Settings";

const page = () => {
  return (
    <Registry>
      <Settings />
    </Registry>
  );
};

export default page;
