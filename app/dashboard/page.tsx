import Dashboard from "@/components/dashboard/Dashboard";
import Registry from "./registry";

const page = () => {
  return (
    <Registry>
      <Dashboard />
    </Registry>
  );
};

export default page;
